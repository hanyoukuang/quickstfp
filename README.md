# quickstfp
此工具配备ssh伪终端、sftp界面。
它封装了asyncssh的部分函数，可以并发的传输文件（夹）。GUI部分使用pyside6显示。
另外它小巧、跨平台性能好，支持Window, Mac, Linux主流操作系统。

## 使用指南：
1. 首先需要安装一个python解释器
2. 打开终端输入
```
pip install asyncssh pyside6
python3 main.py
```
